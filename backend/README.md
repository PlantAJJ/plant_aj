# Plant AJ backend

Python backend scaffold managed with uv.

## Run locally

```bash
uv run python main.py
```

The current entrypoint exposes a small service status helper that can be reused
when an HTTP API is added.
